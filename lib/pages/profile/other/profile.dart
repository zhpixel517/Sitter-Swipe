// someone else's profile that you tap on
// come here from the swipe screen
// use a hero animation

import 'package:flutter/material.dart';

class OtherUserProfile extends StatefulWidget {
  // what kinds of data points do I need to pass through?
  // retreive hobbies and other relevnt profile data when coming from the swipe screen
  const OtherUserProfile({Key? key}) : super(key: key);

  @override
  _OtherUserProfileState createState() => _OtherUserProfileState();
}

class _OtherUserProfileState extends State<OtherUserProfile> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
